name: Repo Sync Faker3

on:
  workflow_dispatch:

  push:
    branches:
      - 'main'
    paths:
      - '.github/workflows/Repo-Sync-Faker3.yaml'

  schedule:
    - cron: 3 */12 * * *

jobs:
  sync-scripts:
    runs-on: ubuntu-latest
    steps:
      - name: Create repository
        run: |
          repo_exists=$(curl -H "Authorization: token ${{ secrets.ACTION_SECRET }}" \
            https://api.github.com/repos/${{ github.repository_owner }}/faker3 | jq -r .name)
          if [[ "$repo_exists" != "faker3" ]]; then
            curl -X POST -H "Authorization: token ${{ secrets.ACTION_SECRET }}" \
              https://api.github.com/user/repos -d '{"name":"faker3"}'
          fi
      - name: Sleep Random Time
        run: sleep $(shuf -i 10-60 -n 1)
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 'lts/*'
      - name: Faker3
        run: |
          bash Scripts/Sync.sh \
            https://github.com/shufflewzc/faker3 main \
            https://${{ github.repository_owner }}:${{ secrets.ACTION_SECRET }}@github.com/xmlySpeed/faker3 main